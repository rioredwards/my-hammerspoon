# Local agent skill symlinks (managed via lnskill on each machine)
.claude/
.codex/
.agents/skills/rio-module-recenter

# Machine-local hotkey overrides (see config/hotkeys.local.jsonc.example)
config/hotkeys.local.jsonc

# Machine-local constant overrides (see config/constants.local.lua.example)
config/constants.local.lua
